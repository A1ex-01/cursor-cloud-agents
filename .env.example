# ===========================================================================
# 环境变量示例文件
#
# 使用方法：
#   1. 复制本文件为 .env         ->  cp .env.example .env
#   2. 填入你自己的真实密钥
#   3. .env 已被 .gitignore 忽略，不会被提交到仓库
#
# 代码中通过 python-dotenv 的 load_dotenv() 自动加载这些变量。
# ===========================================================================

# ---------------------------------------------------------------------------
# E2B 沙箱（https://e2b.dev -> Dashboard -> API Keys）
# 用于创建云端隔离沙箱、执行代码。
# ---------------------------------------------------------------------------
E2B_API_KEY=e2b_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 企业级 / 自托管（BYOC / self-hosted）时，指向你自己的 E2B 集群域名。
# 使用官方 E2B Cloud 时保持注释即可。
# E2B_DOMAIN=e2b.your-company.com

# ---------------------------------------------------------------------------
# 大模型 API Key（任选其一，取决于示例里用的是哪家模型）
# deepagents / langchain 会根据 "provider:model" 前缀选择读取哪个变量。
# ---------------------------------------------------------------------------
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxx
# GOOGLE_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ---------------------------------------------------------------------------
# （可选）LangSmith 追踪 —— 企业级可观测性推荐开启
# ---------------------------------------------------------------------------
# LANGSMITH_TRACING=true
# LANGSMITH_API_KEY=lsv2_xxxxxxxxxxxxxxxxxxxxxxxx
# LANGSMITH_PROJECT=e2b-tutorial
